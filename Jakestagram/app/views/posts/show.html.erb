<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <p><%= @post.text %></p>
    <% if @post.post_picture.attached? %>
    <img src="<%= (url_for (@post.post_picture)) %>" alt="">
    <% end %>


    <h4>Comments: </h4>
    <% @post.comments.each do |comment| %>
    <div>
      <p><%= comment.text %></p>
    </div>
  <% end %>

  <%= render 'shared/comments_form' %>




  </body>
</html>
